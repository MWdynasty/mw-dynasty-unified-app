const {getAccountContext,SUPABASE_URL,SUPABASE_KEY}=require('../../lib/mw-coach-auth');
module.exports=async(req,res)=>{if(req.method!=='PATCH')return res.status(405).json({error:'PATCH only'});try{const c=await getAccountContext(req);if(!['founder_owner','admin','coach'].includes(c.profile.role))return res.status(403).json({error:'Coach access required'});const b=typeof req.body==='string'?JSON.parse(req.body):req.body||{};const w=Math.max(1,Math.min(41,Number(b.currentWeek||1)));if(!b.athleteId)return res.status(400).json({error:'athleteId required'});const r=await fetch(`${SUPABASE_URL}/rest/v1/athlete_program_state?athlete_id=eq.${encodeURIComponent(b.athleteId)}`,{method:'PATCH',headers:{apikey:SUPABASE_KEY,Authorization:`Bearer ${c.token}`,'Content-Type':'application/json','Prefer':'return=representation'},body:JSON.stringify({current_week:w})});const d=await r.json().catch(()=>[]);if(!r.ok)return res.status(r.status).json({error:d.message||'Progression update failed'});return res.status(200).json({ok:true,currentWeek:w,row:d[0]||null})}catch(e){return res.status(e.status||500).json({error:e.message})}};
