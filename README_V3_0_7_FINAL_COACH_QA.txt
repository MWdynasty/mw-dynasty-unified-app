MW Dynasty V3.0.7 — Final Coach QA Patch

Fixes / additions:
- More-menu navigation now binds correctly on rendered pages.
- Live coach search across navigation, assigned athletes, groups, coach programs, and calendar events.
- Live in-app coach notification bell with unread badge and mark-read actions.
- Secure coach invitation creation routed through the MW server API.
- Coach program file import: PDF, Word (.docx), Excel (.xlsx/.xls), CSV, TXT.
- Imported programs are converted to editable text and require coach review before saving.
- Season Calendar database permission fix remains live.
- MW Sprint rep-by-rep tracking remains exclusive to MW Sprint Performance.

Vercel environment variables required for this build:
- OPENAI_API_KEY only.

The Supabase URL and publishable key remain built-in fallbacks for this deployment lineage.
